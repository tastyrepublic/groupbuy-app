query RunInput {
  cart {
    lines {
      sellingPlanAllocation {
        sellingPlan {
          id
        }
      }
    }
    deliveryGroups {
      deliveryOptions {
        handle
        cost {
          amount
        }
      }
    }
  }
}